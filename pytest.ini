[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -v --tb=short -m "not network"
markers =
    network: integration tests that hit live SEC/edgartools (deselected by default; run with -m network)
